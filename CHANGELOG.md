# 1.0.0
 - Support Mixin for ensuring a plugin has an `port_management` method that returns a `Manager` capable of retrieving a `Reservation` that holds a TCP port until `Reservation#convert` or `Reservation#release`.
